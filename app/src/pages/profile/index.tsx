import { Header, Footer, ListPets } from "../../components";

export default function notFound() {
    return (
        <aside>
            <Header/>
            <ListPets/>
            <Footer/>
        </aside>
    );
}